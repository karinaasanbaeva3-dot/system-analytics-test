# Задание 3: Архитектура PUSH-уведомлений

## Блок-схема отправки PUSH-уведомлений
┌─────────────────────────────────────────────────────────────────────────────────┐
│ 1. МОБИЛЬНОЕ ПРИЛОЖЕНИЕ │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ Пользователь открывает приложение │ │
│ │ ↓ │ │
│ │ Приложение запрашивает разрешение на push-уведомления │ │
│ │ ↓ │ │
│ │ Пользователь разрешает │ │
│ │ ↓ │ │
│ │ Приложение получает push-токен от FCM (Android) или APNS (iOS) │ │
│ │ ↓ │ │
│ │ Отправляет токен на бэкенд │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────┘
│
↓
┌─────────────────────────────────────────────────────────────────────────────────┐
│ 2. БЭКЕНД (Notification Service) │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ Получает и сохраняет push-токен в Token Registry │ │
│ │ ↓ │ │
│ │ Слушает события от других сервисов через Event Bus (Kafka) │ │
│ │ ↓ │ │
│ │ Определяет тип события и формирует текст уведомления из шаблонов │ │
│ │ ↓ │ │
│ │ Находит push-токен пользователя в Token Registry │ │
│ │ ↓ │ │
│ │ Отправляет запрос в FCM (для Android) или APNS (для iOS) │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────┘
│
↓
┌─────────────────────────────────────────────────────────────────────────────────┐
│ 3. ВНЕШНИЕ ПЛАТФОРМЫ │
│ ┌────────────────────────────┐ ┌────────────────────────────┐ │
│ │ FCM (Firebase Cloud │ │ APNS (Apple Push │ │
│ │ Messaging) - для Android │ │ Notification Service) │ │
│ └────────────────────────────┘ └────────────────────────────┘ │
│ │ │ │
│ └─────────────┬─────────────┘ │
│ ↓ │
│ Доставляют уведомление на устройство │
└─────────────────────────────────────────────────────────────────────────────────┘
│
↓
┌─────────────────────────────────────────────────────────────────────────────────┐
│ 4. МОБИЛЬНОЕ ПРИЛОЖЕНИЕ │
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ Получает push-уведомление │ │
│ │ ↓ │ │
│ │ Отображает его в шторке уведомлений │ │
│ │ ↓ │ │
│ │ Пользователь нажимает на уведомление │ │
│ │ ↓ │ │
│ │ Открывается соответствующий экран приложения │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────┘


## Источники событий для PUSH (что вызывает отправку)
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ Cart Service │ │ Order Service │ │ Promo Service │
│ (Корзина) │ │ (Заказы) │ │ (Акции) │
│ │ │ │ │ │
│ • Брошенная │ │ • Отмена заказа │ │ • Рекламная │
│ корзина │ │ • Подтверждение │ │ рассылка │
│ │ │ • Статус доставки│ │ • Скидка │
└────────┬─────────┘ └────────┬─────────┘ └────────┬─────────┘
│ │ │
└───────────────────────┼───────────────────────┘
↓
┌─────────────────────────┐
│ Event Bus (Kafka) │
│ Очередь событий │
└────────────┬────────────┘
↓
┌─────────────────────────┐
│ Notification Service │
│ (отправка пушей) │
└─────────────────────────┘


## Типы PUSH-уведомлений и время отправки

| Тип | Приоритет | Когда отправлять |
|-----|-----------|------------------|
| Отмена заказа | Высокий | Сразу |
| Статус доставки | Высокий | Сразу |
| Подтверждение оплаты | Высокий | Сразу |
| Корзина брошена | Средний | Через 1 час бездействия |
| Рекламная рассылка | Низкий | По расписанию (утро/вечер) |

## Обработка ошибок (блок-схема)
┌─────────────────────────┐
│ Попытка отправить пуш │
│ в FCM/APNS │
└────────────┬────────────┘
↓
┌───────────┐
│ Успешно? │
└─────┬─────┘
YES │ NO
┌─────┴─────┐
↓ ↓
┌────────────┐ ┌─────────────────────────┐
│ Уведомление│ │ Повторить через 5 сек │
│ доставлено │ │ (максимум 3 попытки) │
└────────────┘ └────────────┬────────────┘
↓
┌───────────┐
│ Успешно? │
└─────┬─────┘
YES │ NO
┌─────┴─────┐
↓ ↓
┌────────────┐ ┌────────────────────┐
│ Уведомление│ │ Сохранить в │
│ доставлено │ │ Dead Letter Queue │
└────────────┘ │ Отправить алерт │
│ в мониторинг │
└────────────────────┘


## Компоненты архитектуры

| Компонент | Назначение |
|-----------|------------|
| **Notification Service** | Главный сервис отправки пушей, формирует уведомления из шаблонов |
| **Token Registry** | Хранит push-токены пользователей (привязка user_id → push_token) |
| **Schedule Service** | Планировщик для рекламных и утренних/вечерних пушей |
| **Event Bus (Kafka)** | Очередь событий от других микросервисов |
| **FCM / APNS** | Внешние платформы для доставки на устройства |

## Структура хранения Token Registry
┌─────────────┬─────────────┬──────────────┬─────────────┬────────────┐
│ device_id │ user_id │ push_token │ platform │ timezone │
├─────────────┼─────────────┼──────────────┼─────────────┼────────────┤
│ (уникальный │ id │ токен │ ios/android │ Europe/ │
│ идентифика- │ пользователя│ для отправки │ │ Moscow │
│ тор) │ │ │ │ │
└─────────────┴─────────────┴──────────────┴─────────────┴────────────┘
